openapi: 3.0.3
info:
  title: LXP Project Integrated API (Modulith Merged)
  description: 모듈별 파일을 병합하여 생성된 통합 명세 예시
  version: 1.0.0
servers:
  - url: http://localhost:8080
    description: Local Server

components:
  schemas:
    ApiResponse:
      type: object
      properties:
        data:
          nullable: true
        error:
          nullable: true
        success:
          type: boolean
        timestamp:
          type: string
          format: date-time
