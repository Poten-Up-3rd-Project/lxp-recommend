openapi: 3.0.3
info:
  title: "Tag Module Stub"
  version: 1.0.0
  description: "태그 모듈의 paths와 components 조각 명세 예시"
paths:
  /tags:
    get:
      tags:
        - Tag
      summary: 전체 태그 목록 조회
components:
  schemas:
    TagResponse:
      type: object
      properties:
        tagId:
          type: integer
        name:
          type: string